# 보심 AI 기획서 ↔ 실제 사이트(bosim-lab.vercel.app) 검토

> 기획서 9개 섹션 전체를 실제 코드(`legal-ai.html` + `api/*` + `js/*`)와 한 줄씩 대조.
> 전제: 심사위원이 기획서를 읽고 bosim-lab.vercel.app을 직접 클릭한다.

---

## 🔴 심각 — 제출 전 반드시 (URL 클릭 시 바로 드러남)

### 1. 핵심 차별점인 "이의신청 비교" 탭의 UI가 깨져 있음
`legal-ai.html:376-406`의 비교 탭은 `.section-header`, `.section-desc`, `.compare-inputs`, `.input-group`, `.btn-primary` 클래스를 쓰는데 이 클래스들이 `<style>`(8~188행)에 **하나도 정의되어 있지 않음.** 다른 탭은 정돈된 디자인인데 이 탭만 브라우저 기본 스타일(밋밋한 회색 버튼·테두리 없는 입력칸)로 표시됨. 기획서가 "1회 수상작 중 전무한 핵심 차별점"으로 내세운 바로 그 기능이 가장 엉성하게 보임. → **최우선 수정.**

### 2. 기획서에는 있으나 사이트에 아예 없는 "신규" 기능 2개
- **기능 ④ 개정 법령 자동 감지** (3페이지) — 코드에 해당 탭·로직 전무
- **기능 ⑤ 법해석 시각화(흐름도·인포그래픽)** (3페이지) — 코드에 전무

심사위원이 "신규"라고 적힌 기능을 사이트에서 찾으면 없음. → 기획서에서 빼거나, "Phase 2 개발 예정"으로 명확히 분리. (실증형이라 강조한 만큼 없는 기능을 현재형으로 적으면 신뢰도 타격)

---

## 🟠 중요 — 정확성·내부 일관성

### 3. Section 3 API 표가 과장 (8종 나열 vs 실제 4종)
표에는 검색·상세·해석례·시점비교 + **판례(prec)·헌재결정례(ccourt)·중앙행정기관 결정선례(admrul)·행정규칙(admrul)** 까지 8종이 적혀 있지만, 실제 구현은 `api/` 폴더 기준 **검색·상세·해석례·시점비교 4종뿐.** 정작 Section 7은 정직하게 "4종 연동"이라 적혀 있어 **기획서 내부가 모순.** → Section 3을 "구현 4종 / 확장 예정 4종"으로 분리.

### 4. "비교"가 실제로는 diff가 아니라 단순 병렬 표시
기획서: "법령 변경 전후 조문 차이를 즉시 확인". 실제 `extractArticles`(`legal-ai.html:496`)는 두 컬럼에 조문을 나열만 하고 **변경 부분 강조/하이라이트가 없음.** → 표현을 "병렬 비교"로 낮추거나 간단한 변경 강조를 추가.

### 5. 처분시점 조회 정확성 실데이터 검증 필요
`api/law-compare.js:39`는 현행 MST에 `efYd`만 붙여 과거 시점을 조회. 이게 실제로 그 시점의 **과거 조문 텍스트**를 반환하는지 실제 법령으로 검증 필요. 핵심 차별점이라 데모에서 틀리면 치명적.

---

## 🟡 보완 / 정리

### 6. 사이트엔 있는데 기획서엔 없는 기능 — 오히려 플러스 요소
- **서식 진단 탭**(개인정보 수집 적합성, `tab-audit`)
- **AI 실습 / 실습 생성기**(practice.html 등)

→ 작동하는 기능이니 기획서에 한 줄씩 추가하면 실증성 강화.

### 7. 비식별화 기능의 역설 (심사위원이 지적할 포인트)
비식별화 탭은 PII가 포함된 **원문을 그대로 Google Gemini로 전송**해 마스킹함(`runDeidentify`, `legal-ai.html:625`). 마스킹하려고 평문 개인정보를 외부 클라우드로 보내는 구조라, 보훈·공무원 맥락에서 모순으로 보일 수 있음. Phase 2(폐쇄망 로컬)에서 해소되지만, **시범운영 화면에 "실제 개인정보 입력 금지·샘플만 사용" 안내 문구** 필요.

### 8. 해석례 엔드포인트 표기 불일치
기획서는 `lawService.do?target=expc`, 실제 코드(`law-interpret.js:36`)는 `lawSearch.do?target=expc`. 동작은 하나 표기 정정.

### 9. `vercel.json`에 핵심 함수 설정 누락
`vercel.json:3-7`에 law-search/detail/interpret만 있고 **law-compare·gemini가 빠져 있음.** 배포는 되지만 law-compare는 외부 API 3회 호출이라 타임아웃 여유(maxDuration)를 위해 추가 권장.

### 10. 제출 전 확인
커밋 이력에 `disable gemini API endpoint temporarily`가 있음. AI 해석이 핵심이니 **프로덕션에서 Gemini가 실제로 응답하는지** 한 번 확인.

---

## 조치 요약

| 항목 | 유형 | 조치 주체 |
|------|------|-----------|
| #1 비교 탭 UI 깨짐 | 코드 | 즉시 수정 가능 |
| #2 없는 신규 기능 2개 | 기획서 | 빼기 vs 로드맵 이동 결정 필요 |
| #3 API 표 과장 | 기획서 | 4종/예정 분리 |
| #4 diff 아님 | 코드/기획서 | 표현 조정 또는 강조 추가 |
| #5 처분시점 정확성 | 검증 | 실데이터 테스트 |
| #6 누락 기능 | 기획서 | 한 줄 추가(플러스) |
| #7 비식별화 역설 | 코드/기획서 | 안내 문구 추가 |
| #8 엔드포인트 표기 | 기획서 | 정정 |
| #9 vercel.json | 코드 | 함수 추가 |
| #10 Gemini 작동 | 검증 | 프로덕션 확인 |
